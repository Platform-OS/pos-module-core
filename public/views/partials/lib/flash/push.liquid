{% liquid
  if message_key
    assign message = message_key | t
  endif
  assign flash = null | hash_merge: message: message, severity: severity, from: context.location.pathname, now: force_clear

  assign sflash = flash | json
  session sflash = sflash
  return true
%}
